{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "mongodb": "^6.9.0",
    "mongoose": "^8.6.3"
  },
  "scripts": {
    "start": "node app.js",           // To start your app
    "dev": "nodemon app.js",          // For development (requires `nodemon` as a dev dependency)
    "test": "echo \"Error: no test specified\" && exit 1",  // Placeholder for testing
    "build": "echo \"No build step needed\" && exit 0",     // Placeholder for build step (if needed)
    "lint": "eslint .",                 // Run linter (requires eslint as dev dependency)
    "prestart": "npm run lint"          // Optionally lint before starting
  },
  "author": "Your Name",
  "license": "ISC"
}
